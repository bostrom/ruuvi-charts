{
  "name": "ruuvi-charts",
  "version": "1.0.0",
  "description": "Custom dashboard for my RuuviTags",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/index.js --exec babel-node",
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-node": "babel-node --presets=env",
    "build": "babel src -d dist"
  },
  "author": "Fredrik Boström <fredrik.bostrom@iki.fi>",
  "license": "MIT",
  "dependencies": {
    "node-ruuvitag": "^4.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/node": "^7.0.0",
    "@babel/preset-env": "^7.1.0",
    "nodemon": "^1.18.4"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
